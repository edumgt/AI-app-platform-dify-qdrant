{
  "name": "ai-app-platform-monorepo",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm -w apps/api run dev & npm -w apps/web run dev",
    "lint": "npm -w apps/api run lint && npm -w apps/web run lint",
    "db:migrate": "npm -w apps/api run db:migrate",
    "db:seed": "npm -w apps/api run db:seed"
  }
}
